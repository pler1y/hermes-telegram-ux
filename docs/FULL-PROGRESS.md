# Full 主线维护进度

## 当前版本

`1.8.3-rc.2`，Full 主线本地测试候选版，尚未公开发布。保留完整 Telegram 体验；修复停止异常、迟到回合事件、群聊状态归属、步骤结果证据与安装配置保留问题。新增的 Hermes 身份适配集中在 `full_adapter.py`；现有 patch 安装与交付边界见 [FULL-RUNTIME.md](FULL-RUNTIME.md)。

## 2026-09-18

- 修复起点为 `1.8.3-rc.1` / `8e38243614c1ecc37e0cdcbd4e6e223f920ec895`；当前工作区保留 `5283640` 的安装与导航文档修改。修复尚未提交，不以旧 commit 表示本次部署内容。
- 完成停止容错、回合与后台子任务身份隔离、原生会话路由、按操作/尝试归属的进度证据，以及安装语言保留和 YAML 别名隔离。
- 原有 180 项测试在改动前通过；新增回归后每个受支持核心均为 **209 项通过，0 失败、0 错误、0 跳过**，共 1,045 次执行。

| Hermes 核心 | 版本 | 测试 | 兼容/运行/原生生命周期 | 中英文 ZIP 生命周期 |
| --- | --- | --- | --- | --- |
| `b499ab11fe8b081470e269f2fb27abae03000da5` | 0.21.0 | 209 通过 | 通过 | 均通过 |
| `a84a2223f82c3d9906fd4a9d778a188774e7a08e` | 0.21.2 | 209 通过 | 通过 | 均通过 |
| `436ec489854b7110b4c5506b1f52c514fa4c3ace` | 0.21.2 | 209 通过 | 通过 | 均通过 |
| `044a77b3b6af4ce16138d42762f812a20b9f7a89` | 0.21.2 | 209 通过 | 通过 | 均通过 |
| `5eb99eb2844b22ebb723711b8e6a0bbb80bb5f04` | 0.21.2 | 209 通过 | 通过 | 均通过 |

原生生命周期覆盖安装、启用、配置恢复、禁用/移除和接线卸载；ZIP 覆盖完整性、安装、重复安装、运行与卸载恢复。最旧 b499 核心没有官方 validate 命令；其余四个核心的源码、载荷和安装后官方校验均通过，缺失 middleware 声明的负例被拒绝。官方扫描仍将 Full 标为 caution；这不代表 Catalog 准入。

## 专用 Bot 部署

- 沿用现有专用 Bot 和测试账号，通过独立后台测试工具发送消息、点击按钮、校验附件和查询主机进程；无需控制 Telegram 图形客户端。
- 测试环境运行 Hermes 0.21.0 / b499 基线，23 个受保护文件精确匹配。本次仅替换插件，原配置和核心受保护文件哈希保持不变，已保存旧插件与配置备份。
- 已安装插件载荷 SHA-256：`9508a88c256b999f6abc8bfe66f8a4fd6216c6483a17f4b26927681f89fd7c5c`。此哈希覆盖按文件名排序的插件文件 SHA-256 映射，以紧凑 JSON 编码后再次计算 SHA-256。
- Full 已启动，使用同一 Bot 的 Catalog-safe 服务停止，避免两个服务同时接收更新。
- 真实 `core` 场景全部通过：版本入口、首页按钮、精确回复、独立状态消息编辑与真实结果、CSV 附件内容、运行中补充要求、实际停止及后续恢复。报告编号：`20260918-144445-52cc8322`。
- 额外 `stop_restart` 通过：确认旧进程运行后发送停止，随即发起新任务；新任务精确回复，旧进程退出，并观察超过原完成时点，未出现旧完成文件、CSV 或迟到回复。报告编号：`20260918-144841-64178de9`。
- 独立测试工具自身 57 项回归通过；测试报告和部署回执保存在同级 Telegram 测试工具的 `reports` 目录，不进入插件发行包。
- 测试结束后再次核对安装摘要与服务：Full 正常运行、Catalog-safe 未运行、无受控测试进程残留；原生 `/new` 回执确认已切到新会话供继续体验。

## 验证范围

五个核心均完成本地契约与安装验证；真实 Telegram/模型链路只在上述 b499 测试环境执行。群聊、多 Bot 和持久化错误路径由确定性回归覆盖，本轮不把它们计作 Telegram 实机验收。完整私有集成仍按受支持基线维护，未扩大兼容范围。
