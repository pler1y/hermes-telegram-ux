# 安全与隐私

这是在 Hermes 进程中运行的 Python 插件，具有该运行账号的权限，不是沙箱。请先阅读源码，在独立环境验收后使用。

安装器只管理插件文件和列出的交互配置，不读取或复制 OAuth 授权、Bot token 或其他账号凭据。它保存的完整配置备份可能包含私人数据，必须留在自己的受限 Hermes home 中。

自然语言控制和按钮重新进入原生身份及授权检查。按钮绑定用户、聊天、话题和消息，单次使用并在 30 分钟后过期。群聊寻址规则不会因自然短句变成命令而扩大权限。

插件不设置遥测接收地址，也不发送独立进度模型请求。原有 Hermes 模型与 Telegram 通信仍按自身配置工作。状态日志只记录阶段、发送方式、耗时与正文短哈希。公开进度应避免私人原文；自动脱敏不能保证识别所有可能的秘密。

提交问题时仅提供最小脱敏复现。发现疑似凭据泄漏时，应撤销相关凭据，勿将其贴在公共 Issue。仓库发布后请使用托管平台的私密漏洞报告功能。
