name: hermes-telegram-ux-catalog
version: 1.8.3-catalog.1
description: Telegram execution progress from public Hermes events, with optional reply statistics.
author: pler1y
license: MIT
homepage: https://github.com/pler1y/hermes-telegram-ux
requires_hermes: ">=0.21.3"
provides_tools: []
provides_middleware: []
provides_hooks:
  - pre_gateway_dispatch
  - pre_llm_call
  - pre_tool_call
  - post_tool_call
  - pre_api_request
  - post_api_request
  - api_request_error
  - pre_approval_request
  - post_approval_response
  - on_interim_message
  - transform_llm_output
  - on_session_end
  - on_session_finalize
  - on_session_reset
config_schema:
  language:
    type: str
    default: zh
    description: Status language, zh or en. Restart the Gateway after changing settings.
  progress:
    type: bool
    default: true
    description: Show a separate status message for reliably correlated Telegram turns.
  final_summary:
    type: bool
    default: true
    description: Append observed tool and API counts to text replies that used tools.
  update_interval:
    type: float
    default: 1.5
    description: Minimum seconds between status edits (1 to 30).
  status_ttl:
    type: float
    default: 600
    description: Stop showing live status after this many seconds without an event (30 to 3600).
